/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findCommentPda } from "../pdas";
import { DESCHOLARISM_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const ADD_COMMENT_DISCRIMINATOR = new Uint8Array([
  59, 175, 193, 236, 134, 214, 75, 141,
]);

export function getAddCommentDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_COMMENT_DISCRIMINATOR);
}

export type AddCommentInstruction<
  TProgram extends string = typeof DESCHOLARISM_PROGRAM_ADDRESS,
  TAccountComment extends string | AccountMeta<string> = string,
  TAccountPaper extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountComment extends string
        ? WritableAccount<TAccountComment>
        : TAccountComment,
      TAccountPaper extends string
        ? ReadonlyAccount<TAccountPaper>
        : TAccountPaper,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddCommentInstructionData = {
  discriminator: ReadonlyUint8Array;
  content: string;
  commentId: bigint;
};

export type AddCommentInstructionDataArgs = {
  content: string;
  commentId: number | bigint;
};

export function getAddCommentInstructionDataEncoder(): Encoder<AddCommentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["content", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["commentId", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_COMMENT_DISCRIMINATOR }),
  );
}

export function getAddCommentInstructionDataDecoder(): Decoder<AddCommentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["content", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["commentId", getU64Decoder()],
  ]);
}

export function getAddCommentInstructionDataCodec(): Codec<
  AddCommentInstructionDataArgs,
  AddCommentInstructionData
> {
  return combineCodec(
    getAddCommentInstructionDataEncoder(),
    getAddCommentInstructionDataDecoder(),
  );
}

export type AddCommentAsyncInput<
  TAccountComment extends string = string,
  TAccountPaper extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  comment?: Address<TAccountComment>;
  paper: Address<TAccountPaper>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  content: AddCommentInstructionDataArgs["content"];
  commentId: AddCommentInstructionDataArgs["commentId"];
};

export async function getAddCommentInstructionAsync<
  TAccountComment extends string,
  TAccountPaper extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DESCHOLARISM_PROGRAM_ADDRESS,
>(
  input: AddCommentAsyncInput<
    TAccountComment,
    TAccountPaper,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddCommentInstruction<
    TProgramAddress,
    TAccountComment,
    TAccountPaper,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DESCHOLARISM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    comment: { value: input.comment ?? null, isWritable: true },
    paper: { value: input.paper ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.comment.value) {
    accounts.comment.value = await findCommentPda({
      paper: expectAddress(accounts.paper.value),
      authority: expectAddress(accounts.authority.value),
      commentId: expectSome(args.commentId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.comment),
      getAccountMeta(accounts.paper),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddCommentInstructionDataEncoder().encode(
      args as AddCommentInstructionDataArgs,
    ),
    programAddress,
  } as AddCommentInstruction<
    TProgramAddress,
    TAccountComment,
    TAccountPaper,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type AddCommentInput<
  TAccountComment extends string = string,
  TAccountPaper extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  comment: Address<TAccountComment>;
  paper: Address<TAccountPaper>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  content: AddCommentInstructionDataArgs["content"];
  commentId: AddCommentInstructionDataArgs["commentId"];
};

export function getAddCommentInstruction<
  TAccountComment extends string,
  TAccountPaper extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DESCHOLARISM_PROGRAM_ADDRESS,
>(
  input: AddCommentInput<
    TAccountComment,
    TAccountPaper,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddCommentInstruction<
  TProgramAddress,
  TAccountComment,
  TAccountPaper,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DESCHOLARISM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    comment: { value: input.comment ?? null, isWritable: true },
    paper: { value: input.paper ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.comment),
      getAccountMeta(accounts.paper),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddCommentInstructionDataEncoder().encode(
      args as AddCommentInstructionDataArgs,
    ),
    programAddress,
  } as AddCommentInstruction<
    TProgramAddress,
    TAccountComment,
    TAccountPaper,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedAddCommentInstruction<
  TProgram extends string = typeof DESCHOLARISM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    comment: TAccountMetas[0];
    paper: TAccountMetas[1];
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AddCommentInstructionData;
};

export function parseAddCommentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddCommentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      comment: getNextAccount(),
      paper: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddCommentInstructionDataDecoder().decode(instruction.data),
  };
}
