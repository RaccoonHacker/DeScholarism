/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findAuthorProfilePda, findPaperPda } from "../pdas";
import { DESCHOLARISM_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const PUBLISH_PAPER_DISCRIMINATOR = new Uint8Array([
  132, 116, 126, 224, 32, 207, 19, 18,
]);

export function getPublishPaperDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLISH_PAPER_DISCRIMINATOR,
  );
}

export type PublishPaperInstruction<
  TProgram extends string = typeof DESCHOLARISM_PROGRAM_ADDRESS,
  TAccountPaper extends string | AccountMeta<string> = string,
  TAccountAuthorProfile extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPaper extends string
        ? WritableAccount<TAccountPaper>
        : TAccountPaper,
      TAccountAuthorProfile extends string
        ? WritableAccount<TAccountAuthorProfile>
        : TAccountAuthorProfile,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublishPaperInstructionData = {
  discriminator: ReadonlyUint8Array;
  title: string;
  ipfsHash: string;
  metadata: string;
};

export type PublishPaperInstructionDataArgs = {
  title: string;
  ipfsHash: string;
  metadata: string;
};

export function getPublishPaperInstructionDataEncoder(): Encoder<PublishPaperInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["title", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["ipfsHash", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["metadata", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: PUBLISH_PAPER_DISCRIMINATOR }),
  );
}

export function getPublishPaperInstructionDataDecoder(): Decoder<PublishPaperInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["title", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["ipfsHash", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["metadata", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getPublishPaperInstructionDataCodec(): Codec<
  PublishPaperInstructionDataArgs,
  PublishPaperInstructionData
> {
  return combineCodec(
    getPublishPaperInstructionDataEncoder(),
    getPublishPaperInstructionDataDecoder(),
  );
}

export type PublishPaperAsyncInput<
  TAccountPaper extends string = string,
  TAccountAuthorProfile extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  paper?: Address<TAccountPaper>;
  authorProfile?: Address<TAccountAuthorProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  title: PublishPaperInstructionDataArgs["title"];
  ipfsHash: PublishPaperInstructionDataArgs["ipfsHash"];
  metadata: PublishPaperInstructionDataArgs["metadata"];
};

export async function getPublishPaperInstructionAsync<
  TAccountPaper extends string,
  TAccountAuthorProfile extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DESCHOLARISM_PROGRAM_ADDRESS,
>(
  input: PublishPaperAsyncInput<
    TAccountPaper,
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublishPaperInstruction<
    TProgramAddress,
    TAccountPaper,
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DESCHOLARISM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    paper: { value: input.paper ?? null, isWritable: true },
    authorProfile: { value: input.authorProfile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.paper.value) {
    accounts.paper.value = await findPaperPda({
      authority: expectAddress(accounts.authority.value),
      ipfsHash: expectSome(args.ipfsHash),
    });
  }
  if (!accounts.authorProfile.value) {
    accounts.authorProfile.value = await findAuthorProfilePda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.paper),
      getAccountMeta(accounts.authorProfile),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPublishPaperInstructionDataEncoder().encode(
      args as PublishPaperInstructionDataArgs,
    ),
    programAddress,
  } as PublishPaperInstruction<
    TProgramAddress,
    TAccountPaper,
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type PublishPaperInput<
  TAccountPaper extends string = string,
  TAccountAuthorProfile extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  paper: Address<TAccountPaper>;
  authorProfile: Address<TAccountAuthorProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  title: PublishPaperInstructionDataArgs["title"];
  ipfsHash: PublishPaperInstructionDataArgs["ipfsHash"];
  metadata: PublishPaperInstructionDataArgs["metadata"];
};

export function getPublishPaperInstruction<
  TAccountPaper extends string,
  TAccountAuthorProfile extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DESCHOLARISM_PROGRAM_ADDRESS,
>(
  input: PublishPaperInput<
    TAccountPaper,
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublishPaperInstruction<
  TProgramAddress,
  TAccountPaper,
  TAccountAuthorProfile,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DESCHOLARISM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    paper: { value: input.paper ?? null, isWritable: true },
    authorProfile: { value: input.authorProfile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.paper),
      getAccountMeta(accounts.authorProfile),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPublishPaperInstructionDataEncoder().encode(
      args as PublishPaperInstructionDataArgs,
    ),
    programAddress,
  } as PublishPaperInstruction<
    TProgramAddress,
    TAccountPaper,
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedPublishPaperInstruction<
  TProgram extends string = typeof DESCHOLARISM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    paper: TAccountMetas[0];
    authorProfile: TAccountMetas[1];
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: PublishPaperInstructionData;
};

export function parsePublishPaperInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublishPaperInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      paper: getNextAccount(),
      authorProfile: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPublishPaperInstructionDataDecoder().decode(instruction.data),
  };
}
