/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findAuthorProfilePda } from "../pdas";
import { DESCHOLARISM_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIALIZE_AUTHOR_DISCRIMINATOR = new Uint8Array([
  71, 158, 209, 185, 95, 57, 225, 34,
]);

export function getInitializeAuthorDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_AUTHOR_DISCRIMINATOR,
  );
}

export type InitializeAuthorInstruction<
  TProgram extends string = typeof DESCHOLARISM_PROGRAM_ADDRESS,
  TAccountAuthorProfile extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthorProfile extends string
        ? WritableAccount<TAccountAuthorProfile>
        : TAccountAuthorProfile,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeAuthorInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
};

export type InitializeAuthorInstructionDataArgs = { name: string };

export function getInitializeAuthorInstructionDataEncoder(): Encoder<InitializeAuthorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_AUTHOR_DISCRIMINATOR }),
  );
}

export function getInitializeAuthorInstructionDataDecoder(): Decoder<InitializeAuthorInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getInitializeAuthorInstructionDataCodec(): Codec<
  InitializeAuthorInstructionDataArgs,
  InitializeAuthorInstructionData
> {
  return combineCodec(
    getInitializeAuthorInstructionDataEncoder(),
    getInitializeAuthorInstructionDataDecoder(),
  );
}

export type InitializeAuthorAsyncInput<
  TAccountAuthorProfile extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authorProfile?: Address<TAccountAuthorProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: InitializeAuthorInstructionDataArgs["name"];
};

export async function getInitializeAuthorInstructionAsync<
  TAccountAuthorProfile extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DESCHOLARISM_PROGRAM_ADDRESS,
>(
  input: InitializeAuthorAsyncInput<
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeAuthorInstruction<
    TProgramAddress,
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DESCHOLARISM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authorProfile: { value: input.authorProfile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.authorProfile.value) {
    accounts.authorProfile.value = await findAuthorProfilePda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authorProfile),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeAuthorInstructionDataEncoder().encode(
      args as InitializeAuthorInstructionDataArgs,
    ),
    programAddress,
  } as InitializeAuthorInstruction<
    TProgramAddress,
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type InitializeAuthorInput<
  TAccountAuthorProfile extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authorProfile: Address<TAccountAuthorProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: InitializeAuthorInstructionDataArgs["name"];
};

export function getInitializeAuthorInstruction<
  TAccountAuthorProfile extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DESCHOLARISM_PROGRAM_ADDRESS,
>(
  input: InitializeAuthorInput<
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeAuthorInstruction<
  TProgramAddress,
  TAccountAuthorProfile,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DESCHOLARISM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authorProfile: { value: input.authorProfile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authorProfile),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeAuthorInstructionDataEncoder().encode(
      args as InitializeAuthorInstructionDataArgs,
    ),
    programAddress,
  } as InitializeAuthorInstruction<
    TProgramAddress,
    TAccountAuthorProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeAuthorInstruction<
  TProgram extends string = typeof DESCHOLARISM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authorProfile: TAccountMetas[0];
    authority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeAuthorInstructionData;
};

export function parseInitializeAuthorInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeAuthorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authorProfile: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeAuthorInstructionDataDecoder().decode(instruction.data),
  };
}
